# Research Question

How can an AI agent be monitored and restricted when it performs actions such as reading files, using tools, or working inside a controlled project environment?

The main question is whether a simple policy layer and audit log can reduce unsafe behaviour by making every agent action explicit, checked, and recorded.
